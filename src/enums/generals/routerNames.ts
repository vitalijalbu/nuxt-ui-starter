export enum RouterNames {
    DASHBOARD = 'dashboard',
    NOT_FOUND = 'notFound',
    SETTINGS = 'settings',
    SETTINGS_PROFILE = 'settingsProfile',
    USER_ROLES = 'userRoles',
    TAGS_INDEX = 'tags',
}

export enum AuthRoutesNames {
    LOGIN = 'auth.login',
    REGISTER = 'auth.register',
    FORGOT_PASSWORD = 'auth.forgot-password',
}

export enum GlobalRouterNames {
    INDEX = 'users.index',
    TAGS_INDEX = 'tags.index'
}

export enum ActiveRouteNames {
    DASHBOARD = 'dashboard',
    USERS = 'users',
    SETTINGS = 'settings',
    TAGS = 'tags',
}